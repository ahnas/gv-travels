# GV Travels

## 🛠️ Installation & Setup

Install dependencies:
```bash
npm install
```

Run locally:
```bash
npm run dev
```

## 🚀 Deploy

Push changes to Git and deploy to GitHub Pages automatically:
```bash
npm run push
```
*(This runs `git push` followed by `npm run deploy`)*